"""Trade-Assist backend package.

Kept intentionally empty so that importing submodules (e.g. the pure-Python
paper-trading engine in ``app.brokers.paper``) never pulls in heavier optional
dependencies such as FastAPI. Import what you need from the relevant subpackage.
"""
